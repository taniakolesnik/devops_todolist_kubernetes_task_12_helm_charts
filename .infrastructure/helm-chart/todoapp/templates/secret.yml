apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name}}
  namespace: {{ .Values.todoapp.namespace}}
type: Opaque
data:
  {{- range $k, $v := .Values.todoapp.secret.data }}
    {{ $k }} : {{ $v | quote }}
  {{- end }}